# 语言列表

```bash
atom.grammars.getGrammars().map(function (grammar) { console.log(grammar.scopeName); })
```

```json
core:
    customFileTypes:{
        "text.html.basic": "wxml",
        "source.css": "wxss"
    }
```
